class SudokuPuzzleValidator:
    grid = None

    def __init__(self, grid=None):
        self.grid = grid

    # Write Python code for the ‘validate_rows’ method in the SudokuPuzzleValidator class such that it
    # returns ‘True’ if no row contains duplicate numbers and  ‘False’ if at least one does.
    def validate_rows(self):
        pass
    
    # Write Python code for the ‘validate_cols’ method in the SudokuPuzzleValidator class such that it
    # returns ‘True’ if no column contains duplicate numbers and  ‘False’ if at least one does.
    def validate_cols(self):
        pass
    
    # Write Python code for the ‘validate_boxes’ method in the SudokuPuzzleValidator class such that it
    # returns ‘True’ if no 3x3 sub-grid contains duplicate numbers and  ‘False’ if at least one does.
    def validate_boxes(self):
        pass
